name: Discord Notification

on:
  workflow_run:
    workflows: ["Deploy to GitHub Pages"]
    types: [completed]

jobs:
  notify:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect content changes
        id: changes
        run: |
          WIKI_CHANGES=$(git diff --name-status HEAD~1 -- 'content/wiki/**/*.mdx' 2>/dev/null || echo "")
          BLOG_CHANGES=$(git diff --name-status HEAD~1 -- 'content/blog/articles/*.mdx' 2>/dev/null || echo "")

          echo "wiki_changes<<EOF" >> $GITHUB_OUTPUT
          echo "$WIKI_CHANGES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          echo "blog_changes<<EOF" >> $GITHUB_OUTPUT
          echo "$BLOG_CHANGES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          if [ -n "$WIKI_CHANGES" ] || [ -n "$BLOG_CHANGES" ]; then
            echo "has_content_changes=true" >> $GITHUB_OUTPUT
          else
            echo "has_content_changes=false" >> $GITHUB_OUTPUT
          fi

      - name: Send Discord notification
        if: steps.changes.outputs.has_content_changes == 'true'
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          WIKI_CHANGES: ${{ steps.changes.outputs.wiki_changes }}
          BLOG_CHANGES: ${{ steps.changes.outputs.blog_changes }}
        run: |
          BASE_URL="https://tontonsflingueurs.github.io"
          NEWLINE=$'\n'

          WIKI_NEW=""
          WIKI_MOD=""
          BLOG_NEW=""
          BLOG_MOD=""

          # Traitement Wiki
          if [ -n "$WIKI_CHANGES" ]; then
            while IFS=$'\t' read -r status filepath; do
              [ -z "$filepath" ] && continue
              [ "$status" = "D" ] && continue
              if [[ "$status" == R* ]]; then
                filepath=$(echo "$filepath" | cut -f2)
              fi
              TITLE=$(grep -m1 '^title:' "$filepath" 2>/dev/null | sed 's/title: *"\?\([^"]*\)"\?/\1/')
              [ -z "$TITLE" ] && continue
              SLUG=$(echo "$filepath" | sed 's|content/wiki/||;s|\.mdx$||;s|/index$||')
              [ -z "$SLUG" ] && URL="$BASE_URL/wiki" || URL="$BASE_URL/wiki/$SLUG"

              if [ "$status" = "A" ] || [[ "$status" == R* ]]; then
                WIKI_NEW="${WIKI_NEW}• [${TITLE}](<${URL}>)${NEWLINE}"
              else
                WIKI_MOD="${WIKI_MOD}• [${TITLE}](<${URL}>)${NEWLINE}"
              fi
            done <<< "$WIKI_CHANGES"
          fi

          # Traitement Blog
          BLOG_COUNT=0
          BLOG_SINGLE_FILEPATH=""
          BLOG_SINGLE_TITLE=""
          BLOG_SINGLE_URL=""
          if [ -n "$BLOG_CHANGES" ]; then
            while IFS=$'\t' read -r status filepath; do
              [ -z "$filepath" ] && continue
              [ "$status" = "D" ] && continue
              if [[ "$status" == R* ]]; then
                filepath=$(echo "$filepath" | cut -f2)
              fi
              BLOG_COUNT=$((BLOG_COUNT + 1))
              BLOG_SINGLE_FILEPATH="$filepath"
              TITLE=$(grep -m1 '^title:' "$filepath" 2>/dev/null | sed 's/title: *"\?\([^"]*\)"\?/\1/')
              [ -z "$TITLE" ] && continue
              SLUG=$(echo "$filepath" | sed 's|content/blog/articles/||;s|\.mdx$||')
              URL="$BASE_URL/blog/$SLUG"
              BLOG_SINGLE_TITLE="$TITLE"
              BLOG_SINGLE_URL="$URL"

              if [ "$status" = "A" ] || [[ "$status" == R* ]]; then
                BLOG_NEW="${BLOG_NEW}• [${TITLE}](<${URL}>)${NEWLINE}"
              else
                BLOG_MOD="${BLOG_MOD}• [${TITLE}](<${URL}>)${NEWLINE}"
              fi
            done <<< "$BLOG_CHANGES"
          fi

          # Construction du message
          MESSAGE="🚀 **Mise à jour du site !** @everyone${NEWLINE}${NEWLINE}"

          # Section Nouveau
          if [ -n "$WIKI_NEW" ] || [ -n "$BLOG_NEW" ]; then
            MESSAGE="${MESSAGE}═══════════════════${NEWLINE}📗 **NOUVEAU**${NEWLINE}═══════════════════${NEWLINE}${NEWLINE}"
            if [ -n "$WIKI_NEW" ]; then
              MESSAGE="${MESSAGE}**Wiki**${NEWLINE}${WIKI_NEW}"
            fi
            if [ -n "$BLOG_NEW" ]; then
              MESSAGE="${MESSAGE}**Blog**${NEWLINE}${BLOG_NEW}"
            fi
            # Espace après la section Nouveau
            MESSAGE="${MESSAGE}${NEWLINE}"
          fi

          # Section Modifié
          if [ -n "$WIKI_MOD" ] || [ -n "$BLOG_MOD" ]; then
            MESSAGE="${MESSAGE}═══════════════════${NEWLINE}📝 **MODIFIÉ**${NEWLINE}═══════════════════${NEWLINE}${NEWLINE}"
            if [ -n "$WIKI_MOD" ]; then
              MESSAGE="${MESSAGE}**Wiki**${NEWLINE}${WIKI_MOD}"
            fi
            if [ -n "$BLOG_MOD" ]; then
              MESSAGE="${MESSAGE}**Blog**${NEWLINE}${BLOG_MOD}"
            fi
          fi

          # Envoi du webhook Discord
          if [ "$BLOG_COUNT" -eq 1 ] && [ -z "$WIKI_CHANGES" ]; then
            # Un seul article blog : embed avec image cliquable
            BLOG_IMAGE=$(grep -m1 '^image:' "$BLOG_SINGLE_FILEPATH" 2>/dev/null | sed 's/image: *"\?\([^"]*\)"\?/\1/' | xargs)
            if [ -z "$BLOG_IMAGE" ]; then
              BLOG_IMAGE="/blog/default.webp"
            fi
            IMAGE_URL="${BASE_URL}${BLOG_IMAGE}"

            PAYLOAD=$(jq -n \
              --arg content "🚀 **Mise à jour du site !** @everyone${NEWLINE}${NEWLINE}Venez découvrir le nouvel article ! 👇" \
              --arg title "$BLOG_SINGLE_TITLE" \
              --arg url "$BLOG_SINGLE_URL" \
              --arg image "$IMAGE_URL" \
              '{
                "content": $content,
                "embeds": [{
                  "title": $title,
                  "url": $url,
                  "image": { "url": $image }
                }],
                "allowed_mentions": {
                  "parse": ["everyone"]
                }
              }')

            curl -X POST "$DISCORD_WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d "$PAYLOAD"
          else
            # Plusieurs changements : message texte + bannière en attachement
            curl -sL -o banniere.webp "$BASE_URL/wiki/banniere.webp"

            PAYLOAD=$(jq -n \
              --arg content "$MESSAGE" \
              '{
                "content": $content,
                "allowed_mentions": {
                  "parse": ["everyone"]
                }
              }')

            curl -X POST "$DISCORD_WEBHOOK_URL" \
              -F "payload_json=$PAYLOAD" \
              -F "file=@banniere.webp"
          fi
